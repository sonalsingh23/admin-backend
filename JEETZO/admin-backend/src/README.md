Backend `src` scaffold

Structure created:

- `config/` - app configuration
- `controllers/` - request handlers
- `routes/` - express routes
- `services/` - business logic helpers
- `middleware/` - express middleware
- `utils/` - small utilities (logger, etc)

Starter files include `index.js` (server), a health controller and a README.

Next steps:

- Install dependencies (`express`) and run `node src/index.js`.
