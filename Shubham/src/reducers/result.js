const initialState =  0;

const res = (state = initialState, action) => {
    switch (action.type){
        case "SUBMIT": return 
  
        
         default:
              return state;
    }
}
export default res;